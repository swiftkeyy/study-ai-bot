STAGE 1 / FOUNDATION

Что уже добавлено без ломания текущего рабочего бота:
- миграции SQLite под v2.0
- новые таблицы: admins, promo_codes, promo_activations, support_tickets, bans,
  menu_buttons, admin_buttons, bot_features, channels, image_logs, media_requests
- новые поля users и settings
- seed ADMIN_ID в таблицу admins
- база теперь умеет работать с фундаментом для:
  промокодов, бана, поддержки, обязательной подписки, новых кнопок, логов картинок и фото-задач
- admin.py теперь проверяет админа через БД, а не только через один ADMIN_ID в коде

Что не менялось в поведении пользователя:
- главное меню
- AI логика
- платежи
- текущие рабочие handlers

Что заменить на сервере:
- db.py
- config.py
- admin.py
- .env.example (опционально, только для порядка)

После замены файлов:
1) redeploy / restart
2) бот сам домигрирует SQLite
3) проверь /start, меню, AI-ответ, /admin
