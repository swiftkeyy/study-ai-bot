# Pinned to the versions this project is tested with (see tests/).
aiogram==3.22.0
aiohttp==3.12.15
python-dotenv==1.2.3
# Document parsing for task uploads (optional features).
pypdf==6.17.0
python-docx==1.2.0
